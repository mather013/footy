<!DOCTYPE html>
<html>
<div data-role="header" data-position="fixed" data-backbtn="false" data-tap-toggle="false">
  <a href="<%= lp_rounds_path %>" data-theme="a" data-rel="">Back</a>
  <h1><%= 'Last Man Standing 2' %></h1>
</div>

<div class="info-txt" data-role="content">

  Pick a goal scorer of a Premier League match each week.

  <p>
    Only if your player selection scores, can you progress to the next round.

  <p>
    You cannot select the same player more than once per competition.

  <p>
    The winner is the single person left after everyone else is out.

</div>
<%= render 'common/footer' %>
</html>
