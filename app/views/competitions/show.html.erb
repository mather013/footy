<!DOCTYPE html>
<html>
<div data-role="header" data-position="fixed" data-backbtn="false">
  <a href="#" data-rel="back" data-theme="a">Back</a>
  <h1><%= Date.parse(@competition.close_date.to_s).strftime('%b %e %Y') %></h1>
</div>

<div data-role="content" >
  <ul data-role="listview" id = "fix-list">
    <% @fixtures.each do |fixture| %>
    <li>
      <%= "#{ fixture.to_s }" %>
      <br>
      <h10 class="kickoff">
        <%= DateTime.parse(fixture.kickoff.to_s).strftime('%A %e %B %Y %H:%M:%S') %>
      </h10>
    </li>
    <% end %>
  </ul>
</div>
</html>